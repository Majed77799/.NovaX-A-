# Environment variables example for local development
# Copy this file to .env.local (web) and/or .env (Expo) and fill in your keys

# Toggle mocked AI responses (1 = mocks enabled, 0 = real providers)
NOVAX_ENABLE_MOCKS=1

# AI provider keys (set in Vercel Dashboard for Production)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Public URLs for clients
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api
EXPO_PUBLIC_API_URL=http://localhost:3000/api