{
  "name": "novax",
  "private": true,
  "version": "0.1.0",
  "description": "NovaX monorepo for microservices, shared libraries, and infrastructure",
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run -ws --if-present build",
    "dev": "npm run -ws --if-present dev",
    "start": "npm run -ws --if-present start",
    "lint": "npm run -ws --if-present lint",
    "typecheck": "npm run -ws --if-present typecheck"
  },
  "repository": {
    "type": "git",
    "url": "git+https://example.com/novax.git"
  },
  "author": "NovaX",
  "license": "MIT"
}